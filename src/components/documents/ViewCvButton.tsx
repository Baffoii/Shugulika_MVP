"use client";

/**
 * Opens a short-lived watermarked preview in-app (R-021).
 * Original Storage signed URLs are no longer minted for viewers.
 */
export { DocumentPreviewButton, ViewCvButton } from "./DocumentPreviewButton";
