// Test stub for the `server-only` package. In the app it throws if imported into
// a client bundle; in unit tests (node) we alias it here so we can import the
// pure helpers that live alongside server code. See vitest.config.ts.
export {};
