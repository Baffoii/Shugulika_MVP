import { SectionStub } from "@/components/SectionStub";
export const metadata = { title: "Reports" };
export default function Page() { return <SectionStub title="Reports & KPIs" description="Recruiter activity, efficiency, conversion, and outcomes." note="KPIs are computed from the same stage history the dashboard uses. Detailed reporting views are a later-phase feature." />; }
