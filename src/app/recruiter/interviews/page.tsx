import { SectionStub } from "@/components/SectionStub";
export const metadata = { title: "Interviews" };
export default function Page() {
  return (
    <SectionStub
      title="Interviews"
      description="Coordinate screening, recruiter, and client interviews."
      note="Interview records are attached to each application. A consolidated scheduling view is planned; AI async interviews are integration-pending."
    />
  );
}
