import { SectionStub } from "@/components/SectionStub";
export const metadata = { title: "Candidates" };
export default function Page() { return <SectionStub title="Candidates" description="Continent-wide candidate identities." note="Candidates have one global profile; only candidate-approved fields are searchable. Opening an individual private record is permission-checked and audited." />; }
