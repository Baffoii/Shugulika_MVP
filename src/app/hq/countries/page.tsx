import { SectionStub } from "@/components/SectionStub";
export const metadata = { title: "Countries" };
export default function Page() {
  return (
    <SectionStub
      title="Countries"
      description="Country operations and drill-downs."
      note="Tanzania is active in the pilot; the model supports additional African countries. Country comparison views are planned."
    />
  );
}
