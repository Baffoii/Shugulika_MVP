import { SectionStub } from "@/components/SectionStub";
export const metadata = { title: "Users & roles" };
export default function Page() { return <SectionStub title="Users & roles" description="Administer users, roles, and organizational memberships." note="Privileged roles (recruiter, franchise admin, operations, accounts, HQ) are provisioned here and via the documented admin procedure — never through public sign-up." />; }
