/** Shared client-safe document access types (no server-only imports). */
export type DocumentSourceKind = "candidate_document" | "assessment_file";
export type DocumentAccessScope = "preview" | "export";
